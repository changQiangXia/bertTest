# 核心依赖
# 注意：Python 3.7用户请使用 transformers==4.21.0
torch>=1.13.0
transformers>=4.21.0
datasets>=2.0.0
seqeval>=1.2.2
numpy>=1.21.0
tqdm>=4.64.0

# 可选依赖（用于显存优化和训练加速）
accelerate>=0.15.0

# 数据处理（可选，用于数据分析）
pandas>=1.3.0
scikit-learn>=1.0.0

# 解决Python 3.7兼容性问题
importlib-metadata>=4.13.0; python_version<"3.8"
